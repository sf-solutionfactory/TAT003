@model IEnumerable<WF002.Entities.PAGINAT>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TXT50)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PAGINA.URL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SPRA.DESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TXT50)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PAGINA.URL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SPRA.DESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
